/**
 * Input — Kora's text input primitive.
 *
 * Forwards to shadcn's Input but we own the signature so
 * any future swap (Radix, Ariakit, custom) is invisible.
 */
export { Input, type InputProps } from './ui/input'
